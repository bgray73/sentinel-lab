import{mkdir,readFile,rename,writeFile}from'node:fs/promises';import{dirname}from'node:path';import type{PbsData}from'./types.js';
export class PbsStore{constructor(private readonly file:string){}async load():Promise<PbsData>{try{const value=JSON.parse(await readFile(this.file,'utf8'))as Partial<PbsData>;return{snapshots:Array.isArray(value.snapshots)?value.snapshots:[]}}catch(error){if((error as NodeJS.ErrnoException).code==='ENOENT')return{snapshots:[]};throw error}}async save(value:PbsData){await mkdir(dirname(this.file),{recursive:true});const temporary=`${this.file}.${process.pid}.${crypto.randomUUID()}.tmp`;await writeFile(temporary,JSON.stringify(value,null,2),{mode:0o600});await rename(temporary,this.file)}}
